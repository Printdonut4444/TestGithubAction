name: Check Project Status

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]
  workflow_dispatch: # ให้กดรันเองเพื่อเช็คสถานะได้ด้วย

jobs:
  get-status:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Echo Project Status
        # ใส่ GH_TOKEN ไว้ตรงนี้เพื่อให้คำสั่งข้างล่างเรียกใช้ได้
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
        run: |
          echo "🔍 กำลังค้นหาสถานะของ PR #51..."
          # ... คำสั่ง gh project ของคุณ ...
        